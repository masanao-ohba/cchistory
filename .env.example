# Claude Conversations History Viewer Environment Variables

# ポート設定（他のサービスと競合する場合は変更してください）
VIEWER_PORT=18080

# Claude projectsのパス（デフォルトは ~/.claude/projects）
CLAUDE_PROJECTS_PATH=${HOME}/.claude/projects

# 表示するプロジェクトのディレクトリパス（カンマ区切りで複数指定可能、空の場合は全プロジェクト）
# 例: CLAUDE_PROJECTS=/Users/your-username/workspace/project1,/Users/your-username/workspace/project2
# JSON配列形式も使用可能:
# CLAUDE_PROJECTS='[
#   "/Users/your-username/workspace/project1",
#   "/Users/your-username/workspace/project2"
# ]'
CLAUDE_PROJECTS=

# タイムゾーン
TIMEZONE=Asia/Tokyo

# ログレベル (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Next.js Development Indicators
# Controls display of Next.js build activity and React Query devtools
# Default behavior (if not set):
#   - NODE_ENV=development → Show indicators
#   - NODE_ENV=production → Hide indicators
# Set to override default behavior:
#   - SHOW_DEV_INDICATORS=true → Force show in any mode
#   - SHOW_DEV_INDICATORS=false → Force hide in any mode

# Helper Buttons
# Show helper buttons like back-to-top button (true/false)
SHOW_HELPER_BUTTONS=false

# Ngrok Configuration (Required for ngrok tunnel)
# Get your authtoken from https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
# Your ngrok domain (e.g., your-domain.ngrok-free.app)
NGROK_DOMAIN=your-domain.ngrok-free.app

# Ngrok OAuth Settings (Optional - for restricting access)
# Allowed email address (single email, can be used with domain)
NGROK_OAUTH_ALLOW_EMAIL=
# Allowed email domain (single domain, can be used with email)
NGROK_OAUTH_ALLOW_DOMAIN=
